# OpenVan driver manifest — identity readable without importing code.
[driver]
id = "mqtt_homeassistant"
name = "Home Assistant / MQTT"
version = "0.1.0"
kind = "integration"
api = 1
entry = "mqtt_homeassistant"
