# OpenVan driver manifest — identity readable without importing code.
[driver]
id = "modbus_generic"
name = "Generic Modbus RTU/TCP"
version = "0.1.0"
kind = "integration"
api = 1
entry = "modbus_generic"
