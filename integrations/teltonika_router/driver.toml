# OpenVan driver manifest — identity readable without importing code.
[driver]
id = "teltonika_router"
name = "Teltonika RutOS"
version = "0.1.0"
kind = "integration"
api = 1
entry = "teltonika_router"
