# OpenVan driver manifest — identity readable without importing code.
[driver]
id = "chinese_heater"
name = "Chinese diesel heater (blue wire)"
version = "0.1.0"
kind = "integration"
api = 1
entry = "chinese_heater"
