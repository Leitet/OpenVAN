# OpenVan driver manifest — identity readable without importing code.
[driver]
id = "sim_connectivity"
name = "Connectivity Simulator"
version = "0.1.0"
kind = "integration"
api = 1
entry = "sim_connectivity"
